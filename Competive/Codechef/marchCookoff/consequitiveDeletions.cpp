#include <iostream>
#include <cmath>
#include <utility>

#define ll  long long int
using namespace std;

int main()
{ 
	ll tc;
    cin>>tc;
    while(tc--){
        ll n,k; cin>>n>>k;
        ll arr[n];
        for (int i = 0; i < n; ++i)
        {
            /* code */
            cin>>arr[i];
        }

        for(int )
        
    }
    return 0;
}