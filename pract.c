#include <iostream> 
using namespace std; 
  
// Prints max at first position, min at second position 
// second max at third position, second min at fourth 
// position and so on. 
void cd_check(int n,int i,int c)
{ 
    int count=0;
    if()
} 
  
// Driver program to test above function 
int main() 
{ 
    
    int n = 6;
    int i=1;
    int c=6;

    cd_check(n,i,c);
    return 0; 
}